pub struct DustReverbOptions {
    pub a_min: f64,
    pub a_max: f64,
    pub free_dust: bool,

    // pub open_

    pub edge_theta: Vec<f64>,
    pub edge_phi: Vec<f64>,
    pub edge_radius: Vec<f64>,

    pub n_dust: Vec<f64>,
    // pub 
}